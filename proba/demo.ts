/**
 * ПУСКАНЕТО · програмата се отваря в неговия браузър, с една команда.
 *
 * Негово, 11.09 (запис 184): „Искам да ми напълниш всяка функционалност с
 * информация измислена… искам да ми дадеш работещо."
 *
 *     npm run demo
 *
 * Строи, вдига сървъра, отваря браузъра по подразбиране на Профил и остава да
 * върви, докато не се спре с Ctrl+C. Пълненето е БУТОН в самата програма
 * („Напълни с мостра" · `src/mostra/napalni.ts`), не скрит режим тук: човек го
 * натиска сам и вижда разписката какво е записано.
 *
 * Защо не през Playwright: браузърът на проверката е ДРУГ браузър, с друг
 * профил и друго хранилище. Данните, писани в него, не са в неговия Chrome —
 * а програмата е местна и живее в хранилището на браузъра, с който се отваря.
 */

import { spawn } from 'node:child_process';
import { ADRES, pochakaySurvara, pusniServer } from './yadro/server.ts';

async function main(): Promise<void> {
  pusniServer();
  await pochakaySurvara();

  // Windows: `start` е команда на обвивката, не програма · затова през `cmd`.
  const otvarya =
    process.platform === 'win32'
      ? spawn('cmd', ['/c', 'start', '', `${ADRES}#/profil`], { stdio: 'ignore', detached: true })
      : spawn(process.platform === 'darwin' ? 'open' : 'xdg-open', [`${ADRES}#/profil`], {
          stdio: 'ignore',
          detached: true,
        });
  otvarya.unref();

  console.log(`
═══ CORETOVIA ВЪРВИ ═══

  Адрес: ${ADRES}   (отвори го, ако браузърът не се е показал сам)

  1. Прозорецът „Профил" се отваря пръв. Долу има раздел „Мострата".
  2. Натисни „Напълни с мостра" — програмата се пълни с измислени данни
     и показва разписка какво е записано.
  3. Разгледай: Имоти · Управление · Сметки · Служители · Продажби · Настройки.
     Задръж мишката върху име или глава — подсказката казва защо е полето и
     как се смята. Начало · Нормален се сменят от Настройки.

  Данните стоят в този браузър и остават след затваряне.
  Спри сървъра с Ctrl+C.
`);
}

await main();
