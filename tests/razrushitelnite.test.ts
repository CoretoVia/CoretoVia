/**
 * РАЗРУШИТЕЛНИТЕ ВИКАНИЯ · пинът с ръка · и ПЛАНЪТ за всяко.
 *
 * ═══ ЗАЩО СЪЩЕСТВУВА ТОЗИ ФАЙЛ ═══
 *
 * Негово, 10.09.2026: **„«само проби, които може да се изтрият» — това важи
 * за Сметки, но не и за Управление."**
 *
 * Ден по-рано стъпалото на базата 1 → 2 беше написано да ТРИЕ хранилището, а
 * оправданието беше именно онзи негов цитат — изваден извън обхвата си.
 * Изтриването не различава пясъчник от истина: то отнася целия Журнал.
 * Единайсет порти и 553 теста мълчаха, защото нито един не питаше „това нещо
 * трие ли данни".
 *
 * ═══ КАКВО ПАЗИ ═══
 *
 * Обход **Л** на `npm run chestnost` намира всяко разрушително викане в `src/`
 * и `app/`. Прагът е НУЛА — тоест всяко е находка, докато не бъде закачено
 * ТУК, поименно, заедно с плана си.
 *
 * Същата сметка като правило 9 за библиотеките: списъкът е пин с ръка, не
 * догадка на машина. Разликата е, че тук цената не е лиценз, а загубени пари
 * на човек.
 *
 * ═══ КАК СЕ ДОБАВЯ НОВО ═══
 *
 * НЕ се добавя, за да замълчи портата. Добавя се, когато има ПЛАН:
 *   1. какво точно се губи, ако сгрешим;
 *   2. какво се ПРЕНАСЯ вместо да се загуби;
 *   3. как се сверява (правило 7), и какво става при разлика.
 * Ред без трите не влиза.
 */

import { describe, expect, it } from 'vitest';

/**
 * ЗАКАЧЕНИТЕ · файл и ВИД на разрушението.
 *
 * Пинът е по вид, не по номер на ред: номерът се мести при всяко пренареждане
 * и пинът щеше да пада без нищо да е станало. НОВ вид разрушение в същия файл
 * пак е находка.
 */
const ZAKACHENI: readonly string[] = [
  /*
   * 1 · СТЪПАЛОТО НА БАЗАТА 1 → 2 · разрезът (Т39).
   *
   * КАКВО СЕ ГУБИ, АКО СГРЕШИМ: целият Журнал на човека — и Сметки, и
   * УПРАВЛЕНИЕ. Управление е истината (правило 20); тя не се възстановява.
   *
   * ЗАЩО ИЗОБЩО ТРЯБВА: IndexedDB НЕ мени ключа на съществуващо хранилище.
   * Ключът беше `['veriga','seq']`, а трябва да е `['kniga','pisach','seq']`.
   * Забрана тук би била лъжа — затова се брои, а не се забранява.
   *
   * КАКВО СЕ ПРЕНАСЯ: всички записи. `kniga` и `pisach` се извеждат от стария
   * низ; версията им става 0, за да ги провери `hash.ts` със СОБСТВЕНАТА им
   * форма и веригата да остане цяла; несъществувалите тогава полета се КАЗВАТ
   * (`predi-razreza`), а не се измислят.
   *
   * КАК СЕ СВЕРЯВА: брой прочетени срещу брой записани, В САМАТА транзакция.
   * ПРИ РАЗЛИКА: `transaktsiya.abort()` — старата база остава цяла, а
   * отварянето се проваля с думи, вместо да върне половин Журнал.
   *
   * ДОКАЗАНО: `tests/nositel-indexeddb.test.ts` · „стъпалото 1 → 2 · пренася,
   * не трие" — върху ИСТИНСКА база на версия 1.
   */
  'src/nositel/dnevnik-indexeddb.ts · deleteObjectStore',

  /*
   * 2 · ПАМЕТТА НА ЕКРАНА · `vazstanoviEkrana`.
   *
   * КАКВО СЕ ГУБИ, АКО СГРЕШИМ: нищо от Журнала. Тук живеят само поглед и
   * филтър — кой прозорец е отворен, коя колона е свита. Те се пресмятат
   * наново при следващото рисуване.
   *
   * ЗАЩО ТРЯБВА: възстановяване, което само ДОПИСВА, би оставило филтър от
   * предишния поглед — и човек не би разбрал защо вижда по-малко редове.
   *
   * КАК СЕ СВЕРЯВА: не се сверява и не е нужно — нищо не се губи. Границата
   * е ПРЕФИКСЪТ: маха се само онова, което започва с ключа на този прозорец,
   * и никога чужд ключ.
   *
   * ПРИ РАЗЛИКА: няма разлика, която да значи загуба.
   */
  'app/reshetka/pamet-ekran.ts · removeItem',
];

describe('разрушителните викания · пин с ръка', () => {
  it('всяко закачено носи ФАЙЛ и ВИД, разделени с точка', () => {
    for (const z of ZAKACHENI) {
      expect(z).toMatch(/^[a-z0-9/_-]+\.ts · \w+$/);
    }
  });

  it('нито едно не е записано два пъти', () => {
    expect(new Set(ZAKACHENI).size).toBe(ZAKACHENI.length);
  });

  it('списъкът е КЪС · дълъг пин значи, че сме спрели да четем', () => {
    // ПРАГ: ГОРНАТА ГРАНИЦА Е ОТВОРЕНА — числото е днешното, не таван завинаги.
    // Порасне ли, това е повод за разговор, не за вдигане на числото.
    expect(ZAKACHENI).toHaveLength(2);
  });
});
