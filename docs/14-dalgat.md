# 14 · ДЪЛГЪТ · единственият дом на чакащото

**Дата:** 2026-09-08 · **Статус:** жив регистър · **Ред без ДАТА е дефект** (правило 30).

Негово, 08.09: „да не се движи напред, докато нещо назад остава недовършено или нерешено…
да не се оставя нищо в кода за време, което пречи и създава проблеми. Всяко място в кода
и в бъдещото изграждане да има дата за правилното време и правилното място."

## Защо съществува този файл

Одитът на 08.09 (46 агента) намери един и същи клас дефект **пет пъти за 48 часа**:
построено, тествано и **невикано**. Зелен тест над код без викащ ИЗГЛЕЖДА като гаранция и
точно затова е по-опасен от липсващ код.

Диагнозата е поставяна вътрешно **четири пъти** — ADR-015 §6 · ADR-016 §6 · ADR-018 §5 ·
ADR-021 §1 — и болестта се е върнала. Значи всеки досегашен лек е бил **конкретен**, а не
структурен. Този файл е структурният: всяко чакащо нещо има ред, дата и отпушващо условие.

**Правилото за реда:** нов ход не почва, преди предишният да е ЗАТВОРЕН или изрично
ОТЛОЖЕН с дата в тази таблица.

---

## 1 · ОТВОРЕНИ · подредени по тежест

| # | какво | къде | защо чака | КОГА | какво го отпушва |
| :--: | :---- | :---- | :---- | :---- | :---- |
| **Т2** | Правото по четирите оси се пита само от екрана · служител „Вижда само" през Портата ЗАПИСВА | `src/komandi/prozortsi/red.ts:450` (+ `izklyuchi`, `varni`) | не чака нищо · механизмът е построен (`izpalnenie.ts:34,79,101`) | **09.09.2026** | нищо · следващият ход |
| **Т4** | Сверката на сгъването е ТАВТОЛОГИЯ · `ochakvani` се смята като `obshto`, значи е равна винаги | `src/ogledalo/sgavane.ts:88` | не чака нищо · 14 реда | **09.09.2026** | нищо |
| **Т5** | Кранът се пита на входа, не при записа · заспала на опашката операция се записва СЛЕД дърпането | `src/yadro/vrata.ts:414` | не чака нищо · 18 реда | **09.09.2026** | нищо |
| **Т8** | Правило 4 (`actor` в подписа) и правило 11 (NFC) нямат НИТО ЕДИН тест · и двете преживяват мутация | `tests/inv-1-veriga.test.ts` · `tests/vrata.test.ts` | не чака нищо · 62 реда | **09.09.2026** | нищо |
| **Т6** | Четири забрани в слоевете броят по-малко от коментарите си · `src/yadro/index.ts` изнася Вратата и всеки прозорец я взима през барела | `.dependency-cruiser.cjs:58-106` | не чака нищо · 14 реда · **задължително с пробни файлове, които ПАДАТ** | **10.09.2026** | нищо |
| **Т7** | К3 · „замразен низ с контролна сума" не съществува · плюс `tests/katalog.test.ts:30` е твърдение, което не може да падне | `src/komandi/katalog.ts` · нов тест по образеца на `tests/stena.test.ts` | не чака нищо · 25 реда | **10.09.2026** | нищо |
| **Т9** | Обход 4 носи структурно мъртъв израз · търси `''` в текст, от който низовете вече са изтрити. Има жив случай: `src/kniga/chetene.ts:1088` | `stroezh/chistota.mjs:534` | не чака нищо · 12 реда | **10.09.2026** | нищо |
| **Т11** | Печатът на HTML е СТОЙНОСТ, не `Symbol` · обект от JSON може да го подправи и да влезе неекраниран | `app/reshetka/shablon.ts:47-49` | не чака нищо · 8 реда · нула промяна в извикващите | **10.09.2026** | нищо |
| **Т10** | `ts` може да тръгне назад · единственият пазач стои НАД Вратата, значи не важи за `vazstanovi` | `src/yadro/vrata.ts` в `#zapishi` | не чака нищо · 12 реда | **11.09.2026** | нищо |
| **Т3** | Правило 3 · „всеки сбор има ВТОРИ независим път (BigInt)" НЯМА нито едно изпълнение в живия път на парите | `CLAUDE.md:69` срещу `src/smetach/` | **чака НЕГОВО решение**: правилото да стане вярно, или да каже истината (предпазителят `Number.isSafeInteger` е сегашната защита) | **чака дума** | негов отговор · сравнение по правило 24 |
| **Т13** | Прозата няма нито една порта · документи твърдят в СЕГАШНО време неща, които са бъдеще | нов тест по образеца на `tests/rezenite.test.ts` | ~170 реда · най-скъпият, но единственият СТРУКТУРЕН | **12.09.2026** | ходовете дотук |

## 2 · ПОСТРОЕНО И НЕВИКАНО · дългът, който изглежда като гаранция

Правило 30 забранява това да стои без ред. Всяко от тях получава ИЛИ викащ, ИЛИ изрична
дата, ИЛИ ред на екрана, който КАЗВА, че още го няма (правило 12).

| какво | къде | състояние | КОГА |
| :---- | :---- | :---- | :---- |
| `vazstanovi` · 113 реда без викащ · **Журналът не може да се върне от резервно копие** | `src/yadro/vrata.ts:184` | най-скъпото · за местно-първо приложение това е липсващ ИЗХОД | **11.09.2026** · или команда `kniga.vnesiZhurnal` + бутон, или ред „идва с резен N" |
| `zapis.ts` · логът · единствен викащ е тестът | `src/yadro/zapis.ts` | написан 08.09 · чака първия си викащ | **09.09.2026** · закача се към гълтащите `catch` |
| `verigaLichna` · `podpishi` · само от тест | `src/yadro/samolichnost.ts` | адресът е решен, Портата чака | **11.09.2026** · с втората Порта |
| `sabiri` · функцията с преградата за пари | `src/yadro/pari.ts:36` | вика се само от тестове · вж. Т3 | **чака дума** |
| `PoSvoyataVeriga` · построена и тествана, не е закачена | `src/yadro/pravata.ts` | `app/main.ts` подава само `LichnoESamoTvoe` | **10.09.2026** · с Т2 |

## 3 · ЗАТВОРЕНИ · 08.09.2026

| какво | как |
| :---- | :---- |
| **Т1 · спряна секция изяждаше пари от сбора И от изнесената Книга** | `src/smetach/smetki.ts:139` върви по `n.stoynosti`, не по `zhivite(n)` · `Sektsiya` носи `spryana` · спряна БЕЗ редове не се рисува. Два теста, и е **доказано, че падат** при старото поведение |
| четвъртата лъжа · живият адрес `/CoretoVia/` → `/Coretovia/` | `docs/12` · `ADR-022` |
| ключът в ADR-023 §4.2 беше ОТХВЪРЛЕНИЯТ вариант | поправка с доказателство от четири файла |
| Б3 (30-те дни) и Б5 · отговорите бяха в изворите | `docs/10` §Б |
| SECURITY.md · SBOM CycloneDX | нула реда нов код |
| обход 10 · шумът (`console.` · `alert(` · `debugger`) с праг НУЛА | `stroezh/chistota.mjs` |
| самоличността · Ed25519, проверена в четири среди включително `file://` | `src/yadro/samolichnost.ts` |
| `VsichkoRazresheno` падна · границата е на място ПРЕДИ първото събитие | `app/main.ts` |

## 4 · КОЕ НЕ СЕ ПРАВИ · назовано, за да не се преоткрива

| какво | защо НЕ |
| :---- | :---- |
| дублираното на прага (`izraz.ts:159` · `chettsi.ts:61` · `sluzhiteli.ts:91`) | НА прага, не над него · сливането ще влоши четимостта повече, отколкото купува |
| преномериране на правилата | CLAUDE.md сам го забранява и е прав · номера се сблъскват |
| К3 да стане отделен обект вместо тип | прегради се строят, когато агентът е ОТДЕЛЕН процес · контролната сума купува 80 % за 25 реда |
| пренасяне на 14-те външни ADR-а | скъпо · евтиното е ЕДИН ред в `00-CHETI-PARVO.md` |
| `npm run proba` два пъти преди ВСЕКИ коммит | 232 × 2 стъпки за рядък дефект · един ред в `package.json` за CI стига |
| атомарен внос в `vazstanovi` | 45 реда за път без викащ · идва ЗАЕДНО с бутона, не преди него |

---

## Как се чете този файл

**Ред без дата е дефект.** Ако нещо се отлага, то получава дата и отпушващо условие тук —
не коментар в кода, не „после", не мълчание.

**Затвореното слиза в §3 със ЗАПИСА как е затворено**, не се трие: правило 1 важи и за
този регистър.
