# 18 · Отговорите от 08.09 вечерта · и какво намери проверката

**Дата:** 2026-09-08 · **Вид:** думи-огледало · **Състояние:** запис · **Статус:** отговорите са негови и са затворени · находките чакат ред

Той отговори на М1 · М2 · М3 и отвори два нови отдела. Всяко твърдение долу е **проверено
с команда**, не преценено. Двайсет агента четоха кода по десет оси.

---

## 1 · НАЙ-ГОЛЯМАТА НАХОДКА · Книгата му вече го е казала

Той поиска правото над редовете да се дава **по секции**. Претърсването намери това:

```
src/model/osnova.ts:1070   tekst('redove', 'достъп до Секци Редове'),
zadanie/05-sluzhiteli.md:60   клетка E16 · същият низ, негов
```

**Главата на оста „редове" се казва „достъп до Секци Редове" — от самото начало, с
неговата ръка.** Не е ново изискване. Кодът просто никога не е прочел собствената си глава.

Това е трети случай по правило 22, но обърнат: този път изворът не отговаряше на въпрос,
а **описваше функция, която не е построена**. Затова се записва тук, а не като цена.

---

## 2 · ПРАВИЛО 21 НЕ ТРЯБВА ДА ПАДА

Негово: „Ако се налага може да падне правилото, ако няма друг вариант… То е основна и
модерна функционалност за бизнеса и живота."

**Не се налага.** Ето защо, дословно от конституцията:

| какво пише | какво значи |
| :---- | :---- |
| Правило 21: „Поверителните данни живеят при клиента — устройството и неговият Драйв, **никога при нас**." | Забранява данни **ПРИ НАС**. Не забранява мрежа. |
| „без мрежа при работа" | Това е в **правило 9** и се отнася за **БИБЛИОТЕКИ**, не за приложението |
| `ADR-023:36` | „**ИИ иска мрежа.** Затова той е в ОНЛАЙН пакета и го НЯМА в локалния" |

**Прецедентът вече е приет.** ADR-023 решава точно този случай за ИИ: клиентът си носи
ключа, плаща на своя доставчик, „ние не държим чужд ключ, не плащаме чужда сметка".
Поканата през неговия Гугъл е **същата сделка**: неговият акаунт, неговият токен, неговото
устройство, нула байта при нас.

**Честната граница, казана веднага:** проектът има **два пакета**. Локалният има
`connect-src 'none'` — там мрежа няма по стена, не по избор (`ADR-023:180`). Значи:

- **онлайн пакет** → програмата се свързва с неговия Гугъл/Майкрософт/Епъл и праща поканата
- **локален пакет** → поканата пада на ръчния вариант: програмата дава кода, той го праща

Едно правило пада само ако няма друг път. Тук има път, и той е вече утъпкан.

---

## 3 · М1 = В · и сблъсъкът, който отговорът разкри

Негово: и живо, и замразено · плюс **бутон „към живите данни"** в Сметки · плюс **Отчети**
със сценарии за период напред, коефициенти, диаграми и графики.

**Находка:** думата „модел" вече значи нещо ДРУГО в проекта.

| | какво значи ДНЕС | какво значи в неговите думи |
| :---- | :---- | :---- |
| източник | `ADR-014:19` | негово, 08.09 |
| определение | **именуван ПОГЛЕД** — тактът, периодът, филтърът, скритите, избраните сметки | **вариант на ДАННИТЕ** — сценарий за период напред, който се смята и сравнява |
| пипа ли данни | **„Не е данни: данните са в Журнала и моделът не ги пипа."** | смята върху тях |

И втора, по-твърда пречка: `ADR-014:36` — **„Журналът пази карта ключ → стойност; какво
значат ключовете, знае само екранът."** Снимката е НЕПРОЗРАЧНА за домейна. Тоест днес
нищо в сметача не може да смята върху модел, дори да поиска.

**Доброто:** командите `ekran.zapaziModel` и `ekran.mahniModel` са РОДОВИ, вече приемат
`prozorets: 'smetki'`, и шест зелени теста го доказват. **Портата е готова за Сметки;
екранът не е.**

**Следствие:** нуждаят се ДВЕ различни неща с две различни имена. Едното е поглед
(построено), другото е сценарий (не е). Да носят едно име значи да се счупят взаимно.

---

## 4 · М3 · състоянията се натрупват — и това ВЕЧЕ е построено веднъж

Негово: „Може да е на няколко неща като придобива нови състояния… те се развиват по
различен начин, както е нормално да бъде."

**Проверката намери живия образец:**

| същност | колко състояния носи ДНЕС | адрес |
| :---- | :--: | :---- |
| Имот | 1 · `sastoyanie` | `osnova.ts:328` |
| **Обект** | **2** · `kategoriya` И `vid` — **и двете се казват „Състояние"**, второто се номерира ВЪТРЕ в първото | `osnova.ts:343` |
| Бизнес | 1 · своя трета номенклатура | `osnova.ts:366` |
| Задача | 2 · `vid` и `otsenka` | `osnova.ts:403` |
| **Движение** | **4** · `sektsiya` · `sektsiyaR` · `funktsiya` · `sastoyanie` | `osnova.ts:515` |

**Натрупването не е ново — Обектът го прави от първия ден.**

**Но има стена, и тя е нарочна:** клетката е ЕДИН скалар. `eKletka` иска точно един слот
(`kletka.ts:45`), а регистърът отказва с думи: „Клетката трябва да носи точно един слот"
(`registar.ts:117`). Тоест **много стойности в ЕДНА колона са активно ЗАБРАНЕНИ**, не
просто непостроени.

**Значи моделът е ясен и не иска нова машина:**

> Състоянията се натрупват като **отделни КОЛОНИ**, всяка със своя номенклатура —
> точно както Обектът вече носи две. Не като списък в една клетка.

Това пази Вратата, пази Книгата (една клетка = една стойност, четима в Ексел) и пази
правило 28. И отговаря на думите му: „развиват се по различен начин" — различна колона,
различна номенклатура, различен живот.

---

## 5 · УМЕНИЯТА · новият отдел пада точно върху К3 и дълга Т7

Негово: умението се **създава · качва · трие**, но **не се редактира**; основните умения
живеят **В КОДА** и забраняват на ИИ да ги мени.

**К3 обещава три неща. Построени са ДВЕ:**

| обещанието на К3 | построено? | адрес |
| :---- | :--: | :---- |
| агентът получава тип без `izpalni` | ✔ | `src/porta/porta.ts:28` |
| предлага чрез `dryRun`, човекът приема | ✔ | — |
| **основните команди са замразен низ с контролна сума** | **✘** | дълг **Т7** |

**И преградата е по-слаба, отколкото звучи.** `PortaZaChetene` е САМО ТИП — преграда при
компилация, не при работа. Собственият ни тест го доказва:

```
tests/porta.test.ts:31-33
  const samoChete: PortaZaChetene = iz;   // същият обект
  // @ts-expect-error
  expect(typeof samoChete.izpalni).toBe('function');   // izpalni СЪЩЕСТВУВА
```

Тоест агент през JS, през `as any`, или получил целия `Izpalnitel` — **пише**.

**Образецът за „замразен низ" обаче вече съществува:** `PINAT` в `tests/stena.test.ts`
пази целия CSP низ, пинат с ръка. Същият похват върши работа и за основните умения.

**И едно решение, което новият отдел ОБРЪЩА.** `docs/14-dalgat.md:130` казва в раздела
„КОЕ НЕ СЕ ПРАВИ": *„К3 да стане отделен обект вместо тип — прегради се строят, когато
агентът е ОТДЕЛЕН процес."* Но агент, който **качва файлове с умения**, е много по-близо
до отделен процес, отколкото беше вчера. **Сметката се мени и решението трябва да се
преразгледа** (правило 27).

---

## 6 · КАКВО НАМЕРИ ПРОВЕРКАТА, БЕЗ ДА Е ПИТАНА · и това е сигурност

Двайсетте агента намериха дупки, за които никой не е питал. Подредени по тежест:

| # | находка | адрес |
| :--: | :---- | :---- |
| **1** | **ТРИ от четирите оси не пазят нищо — но СЕ ЧЕТАТ, за да се ПОКАЖАТ.** `dostapaMi` (`pravo.ts:232`) чете и четирите и Профилът ги рисува (`app/prozorets/profil.ts:32`), а нищо не ги прилага. Тоест не е мълчание — **екранът обещава правило, което кодът не пази.** Профилът дори пише: „каквото Длъжността не дава, не се отваря от тук" — невярно за три от четирите оси | `pravo.ts:232` · `app/prozorets/profil.ts:48` · `app/main.ts:99` |
| **2** | **Оста „хедъри" не се пита от Портата** — `mozheDaRedaktira` се вика само от екрана (`app/prozorets/smetki.ts:44`), нула викащи в `src/komandi/`. Това е ТОЧНО дупката, която затворихме за оста „редове" (Т2), още отворена тук | `pravo.ts:200` |
| **3** | **Гейтът за „Вкарване" е ВСИЧКО-ИЛИ-НИЩО** — `v.sektsii.every(...)`. Право върху две от трите секции днес дава **нула**. Това е буквално обратното на исканите тикчета по секция | `app/prozorets/smetki.ts:161` |
| **4** | **12 от 25 команди нямат НИКАКВА проверка на правото** — включително онези, които РАЖДАТ и СПИРАТ секции (`nastroykiDobaviStoynost` · `nastroykiSpriStoynost`), плюс `smetkiZapishiKesh` · `smetkiZapishiDds` · `knigaIznesi` · `knigaVnesi` · `obshtoStorno` | броено: само `red.ts` (5) и `sluzhiteli.ts` (3) имат `koyMozhe` |
| **5** | **Настройки е отворен за всеки.** Нула `koyMozhe` в `nastroyki.ts`, а прозорецът се казва „Настройки(**Стопанин**)". Всеки, който пише ред, може да добави или спре секция | `src/komandi/prozortsi/nastroyki.ts` |
| **6** | **И четирите оси са СВОБОДЕН ТЕКСТ.** `tekst()` дава `zatvorena: false`, без номенклатура. Правописна грешка разширява правото **тихо**. А затвореният вход съществува и се ползва другаде — колоната „Длъжност" е `izbor` с номенклатура | `osnova.ts:1068-1071` срещу `osnova.ts:1062` |
| **7** | **Профилът и правото казват РАЗНО за един и същ човек.** `dostapaMi` не знае за Стопанина и не знае правилото „човек без ред ВИЖДА": за непознат имейл връща Скрито×4, а `pravotoNaImeyla` за същия човек връща „вижда". **Два ЖИВИ теста твърдят грешното** | `pravo.ts:223` срещу `pravo.ts:160` |
| **8** | ~~Смяната на секция ражда НОВ ред, защото секцията е `vKlyucha: true`~~ **ОБОРЕНО от скептика.** `vKlyucha` е **МЪРТЪВ ФЛАГ**: 14 поставяния, **нула четци**. Поведението беше изведено от код, който никой не изпълнява — и това вече стои оборено в `docs/12:503`. Вторият мъртъв флаг е `vid: 'zaklyucheno'` (11 номенклатури го носят, нула го четат) | `src/model/kolona.ts:103` · `osnova.ts` ×14 |
| **9** | Обликът: в целия `app/` има **ТРИ отметки**, писани по **ДВА различни образеца**, нито една не е функция, и **нула CSS реда** ги стилизират (`accent-color` → 0). Падащото меню има общ дом (`poleSIzbor`), но се гради на **шест места**, пет от тях на ръка | `app/prozorets/ii.ts:133` · `imoti.ts:41` · `smetki.ts:310` |
| **10** | Секциите са **12 базови**: 4 приходни (Наем Банка · Наем Кеш · Бизнес · Други) и 8 разходни (Заплати Кеш · Фактури Кеш · Фактури Карта · Фактури Банка · Кредити · Банкови такси · Заплати Банка · Бизнес). Броено от команда | `osnova.ts:222` |

---

## 6б · ДОКАЗАНО С ИЗПЪЛНЕНИЕ · не с четене · това вече не са въпроси

Скептиците не се задоволиха с `grep`. Пуснаха **истинския `Izpalnitel`** и записаха в
Журнала. Ето какво мина:

| # | какво беше пуснато | резултат |
| :--: | :---- | :---- |
| **Ж1** | **Наблюдател** (Длъжност №5, и четирите оси „Вижда") изпълни `smetki.zapishiKesh` | **ЗАПИСА СЕ** · 5000,00 в кеша за 2026-09 |
| **Ж2** | Наблюдател изпълни `smetki.zapishiDds` | **ЗАПИСА СЕ** · начислен ДДС 123,45 |
| **Ж3** | Наблюдател изпълни `nastroyki.spriStoynost` | **ЗАПИСА СЕ** · разходна секция №5 „Кредити" стана СПРЯНА |
| **Ж4** | Наблюдател изпълни `nastroyki.preimenuvayStoynost` и преименува „Кредити" на „**Кеш**" | **ЗАПИСА СЕ** — и с това **РАЗДАДЕ ПРАВО** |

**Ж4 е веригата на ескалацията и тя е жива.** `pravo.ts:213` сравнява с
`obhvat.includes(duma)` — **съдържане на подниз, не съвпадение на дума**. Обхватът на
Помощник Управителя е „хедъри: заплати, фактури кеш, фактури карта". Проверено с
изпълнение: секция на име **„Кеш" → true** · „Фактури" → true · дори „Хедъри" → true.

> Всеки, който може да пише ред, създава секция на име „Кеш" — и Помощникът получава
> право върху нея, без никой да му го е давал.

**И две ПРАЗНИ ИСТИНИ отварят право вместо да го стеснят:**

| къде | какво става |
| :---- | :---- |
| `pravo.ts:210-213` | `.filter((x) => x.length > 2).every(...)` · празен масив → **`true`**. Хедър с празно име минава |
| `app/prozorets/smetki.ts:161` | Гардът на „Вкарване" е **FAIL-OPEN**: `v.sektsii` се строи по съвпадение на ТЕКСТ срещу три зашити низа с `.filter(x !== undefined)`. **Преименувана секция изпада мълчаливо**; изпаднат ли и трите, списъкът е празен и `[].every(...)` → **`true`** |

**И лъжливото обещание, живо за базова Длъжност.** Сондата с Помощник Управител даде
едновременно:

- `mozhePriVkarvane = TRUE` → екранът изписва **„Имаш право да вкарваш тук."** и рисува клетките редактируеми
- `zashtoNeRedaktiraRedove` = **„Ти само гледаш редовете"** → Портата отказва

Екранът кани, Вратата отказва. **Това е обяснението на Т14** — не е липсващо право, а две
оси, които не си говорят.

**Още три, по-тихи:**

| # | находка | адрес |
| :--: | :---- | :---- |
| **Ж5** | **Два реда за една Длъжност → печели НАЙ-СТАРИЯТ.** Правило 1 казва „поправка = ново събитие"; поправи ли някой достъпа с НОВ ред вместо с `red.popravi`, старият продължава да важи | `pravo.ts:102-112` · `tablitsa.ts:61` |
| **Ж6** | **Стопанинът се вижда като „Скрито"×4 в собствения си профил.** Заобикалянето му го има на три места, но не и в `dostapaMi` | `pravo.ts:223` срещу `:158` · `:200` · `:262` |
| **Ж7** | **Клетката „кам" избягва гарда дори когато той работи** — `redHTML` ѝ дава `data-redakt` БЕЗУСЛОВНО, за разлика от съседката ѝ | `app/prozorets/smetki.ts:167-172` срещу `:174` |

**Ж1–Ж4 не са дизайн, а дефекти.** Влизат в дълга като **Т27–Т30** и се поправят преди
всякакви тикчета — правило 30: не се строи върху нездраво.

---

## 6в · ТРИ НАХОДКИ, КОИТО РЕШАВАТ ВЪПРОСИТЕ ОТ `docs/17`

| находка | кой въпрос решава |
| :---- | :---- |
| **„Бизнес" е секция И в Приход (`osnova.ts:225`), И в Разход (`:239`).** Значи изречение „Редактира секции: …, Бизнес, …" е **двусмислено** и не се разрешава по име | **С2** · правото трябва да носи и СТРАНАТА, не само името. Вариант „само по секция" пада |
| **Няма вид колона „отметка" в целия Модел.** `VidKolona = VidStoynost \| 'izbor' \| 'vrazka' \| 'nomeratsiya'`; тикче като КЛЕТКА не съществува — всички тикчета днес са екранни | **С5** · тикчето или става нов вид колона, или остава екранна контрола над изречението |
| **ЗАТВОРЕНАТА колона е изходът.** `shema.ts:90-91` + коментарът на `:83`: „Затворените колони изобщо не са в схемата" — колона със `zatvorena: true` **не чупи нито един писач**. А обратното е капан: `required.push` стои ИЗВЪН условието `zadalzhitelna` (`shema.ts:93`), значи дори НЕЗАДЪЛЖИТЕЛНА нова колона иска ключа си в товара | **С4** · има безопасен път и той е точно един |

**И четвърти пазач, който не бях преброил:** мострата пинва лентата „Достъп на Длъжности
за Служител" и **шестте ѝ глави като данни** (`tests/mostri/mostra-kniga.ts:1024`), а тя
се чете от четири теста и от `stroezh/`. Нова колона в `dostap` чупи и нея.

---

**И едно агентско твърдение, което ПАДНА.** Един от агентите докладва „59 тест-файла, 528
теста". Броено на място: **58 файла, 526 теста**. Числото се брои от командата, не се
преписва — включително от агент (правило 14 · правило 27).

**Находка 3 е най-важната за днешния му въпрос:** днешният гейт прави точно обратното на
това, което той иска. Не се добавя функция — **поправя се посока.**

---

## 7 · КАКВО СЕ ЗАТВАРЯ И КАКВО СЕ ОТВАРЯ

**Затворени с негова дума:** М1 (= В) · М2 (= А автоматизирано) · М3 (натрупване) ·
А3 · А4 частично · **Б4** („уволнение: спира служебният, остава личният").

**Отворени нови:**

| # | какво | защо чака |
| :--: | :---- | :---- |
| **У1** | Думата „модел" значи ДВЕ неща. Кое име носи сценарият? | §3 · две неща с едно име се чупят взаимно |
| **У2** | Умението е предмет, който се трие, но не се редактира — къде живее? В Журнала (само за добавяне) ли, или като файл? | §5 |
| **У3** | „Основните умения в кода" — по образеца на `PINAT`? И решението в `docs/14:130` да се преразгледа ли? | §5 · правило 27 |

**Дългът порасна с находките от §6.** Те не са питане — те са дупки и влизат в
`docs/14-dalgat.md`.
