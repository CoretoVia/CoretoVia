# 35 · ДОВЪРШВАНЕТО · 1893 неща, отворени поименно

**Дата:** 2026-09-10 · **Вид:** проучване · **Състояние:** запис · **Ражда:** ход 9 · нов клас дефект

---

## 1 · ЗАЩО ГО ИМА ТОЗИ ДОКУМЕНТ

Вчерашното мерене завърши честно: всеки от осемте четеца обяви какво **НЕ Е** измерил.
Инварианти — 26 от 114. Изисквания — 45 от 1221. Книгата — нито една клетка. И осемте
завършиха с едно и също: **нищо не беше ПУСНАТО**, защото четците са само за четене.

Негово, 10.09: **„Остави ги да си продължат работата и да я завършат."**

Тридесет и пет довършиха. Числата долу са БРОЕНИ (правило 14) — от дневника на потока, не
преписани.

| парче | агенти | отворени ПОИМЕННО | потвърдени | **ОСТАРЕЛИ** | **ОБРАТЕН ДЕФЕКТ** |
| :---- | :--: | ---: | ---: | ---: | ---: |
| Инвариантите А–И | 9 | **263** | 173 | 41 | 38 |
| Заданието · 19 раздела | 19 | **1248** | 481 | 55 | 41 |
| Книгата · 4 листа | 4 | **276** | 173 | 2 | 3 |
| Регистърът · адресите | 1 | **76** | 50 | 26 | 1 |
| Сметачите · верността | 1 | **18** | 4 | 1 | 1 |
| Портите · ПУСНАТИ | 1 | **12** | 7 | 6 | 1 |
| **общо** | **35** | **1893** | **888** | **131** | **85** |

**443 находки · 86 от тях НОСЕЩИ.**

Вчера бяха отворени поименно около седемдесет неща. Днес — 1893.

---

## 2 · ПЪРВО ДОБРОТО · всичко е ЗЕЛЕНО, и вече е ПУСНАТО

Дванайсетте порти не бяха пускани от нито един четец. Пуснати са:

```
npm run proverka   EXIT=0 · 12 порти · 61 тестови файла · 586 теста · 58,93 s
  chistota         116 файла код · 63 теста · 597 изнесени имена · 12 обхода · нито един над прага
  chestnost        12 обхода А–Л · всичките НУЛА
  sloeve           ✔ no dependency violations (121 модула · 584 зависимости)
  litsenzi         202 пакета · 0 извън списъка
  dumi:proveri     42 = 42 · разлика 0
  stil             222 файла · без поправки
  registar         187 въпроса · 76 отговорени · 65 непознати · 29 решени от кода
  protokol         7 вида работа · 22 пътя · 22 на диска
  karta:proveri    92 описани документа · сверена с дървото

npm run proba      237 стъпки · НУЛА находки · ДВА ПЪТИ ПОРЕД
npm run build      EXIT=0 · 117 модула · 19 файла · 1242,2 KB
```

**И това затваря един дълг с измерване, не с довод.** Т35 („двата пакета са обещание, не
изход на строежа") е решен в другата посока от очакваното: строежът дава **ЕДИН** изход,
`dist/`, и толкова. Няма два пакета, които да се разминават — няма и втори пакет.

---

## 3 · НОВИЯТ КЛАС · пазач, който е НАДПИС

Вчерашното мерене гледаше редовете с **„НИКОЙ"** — там, където архитектурата сама признава,
че нищо не пази правилото. Днешното гледа и **обявените за ПАЗЕНИ**. Никой досега не ги е
поглеждал, и точно там живее по-опасното:

> **„НИКОЙ" е честно. „ПАЗЕН" с пазач, който не може да падне, е ЛЪЖА вътре в
> архитектурата — правило 30, обърнато наопаки.**

**Осемдесет и пет** такива. Ето най-тежките, всеки проверен и от мен поотделно:

### 3.1 · Обход 6 на `chistota` НЕ ЛОВИ невиканото (инвариант А24)

Портата е зелена и казва „нито един обход над прага си". А `napraviZapisvach`
(`src/yadro/zapis.ts:156`) има **един** дом и **двайсет** викания — всичките в
`tests/zapis.test.ts`. Нула в `src/komandi/`, нула в `src/porta/`, нула в `app/`.

Причината е механична: обход 6 брои **файлове**, които никой не внася, а `zapis.ts` се внася
от барела `src/yadro/index.ts:18`. Обход 1 („мъртво") пък брои и препратките от `tests/` за
употреба. Тоест **правило 30 минава през портата, обявена за негов пазач.**

Същият обход докладва и **32 изнесени имена, чийто единствен викащ е тест**, и **47 файла,
които нито един тест не внася** — и двата брояча са БЕЗ ПРАГ. Зелената порта СЪДЪРЖА дълга и
не може да го зачерви.

### 3.2 · Правило в слоевете, което пази НЕСЪЩЕСТВУВАЩА папка (инвариант А29)

`.dependency-cruiser.cjs:87-92` носи правилото `agentat-ne-pishe` и пази пътя `^src/agenti/`.

```
$ ls -d src/agenti
ls: cannot access 'src/agenti': No such file or directory
```

`npx depcruise` минава **празно** през него — няма какво да съвпадне. К3 („агентът не пише")
се пази от типа `PortaZaChetene` и от `@ts-expect-error` в теста; третият му пазач е надпис.

### 3.3 · Моделът не се проверява в живия път (инвариант В2 · изискване М12-04)

`proveriModela` има **едно** попадение в `src/` и `app/` заедно — самата декларация. Всичките
му викащи са тестове. Значи невалиден Модел се приема в производството мълчаливо.

### 3.4 · „Осемте прозореца" се пазят само в половината дърво (инвариант В1)

`tests/osemte.test.ts:59` е буквално `obhod('src')`. Заглавието му на ред 42 само признава:
„никъде другаде **в src/**". `app/` не се обхожда. К1 се пази в половината къща.

### 3.5 · Правото стига до Портата само по ДВЕ от четирите оси (инварианти Г8 · Д10)

От четирите оси на достъпа жив пазач имат **две** — `redove` и `hedari`. Обявените за пазени
`tabove` и `zhurnal` нямат нито един. И `PoSvoyataVeriga` (`src/yadro/pravata.ts:134`) има
**нула** попадения в `app/`, `src/komandi/` и `src/porta/` — потвърдено втори път след
вчерашния скептик, който не можа да го обори.

---

## 4 · КОНСТИТУЦИЯТА НАРУШАВА СОБСТВЕНОТО СИ ПРАВИЛО 14

`CLAUDE.md` носи три броени числа. И трите са остарели, и то в пъти:

| в CLAUDE.md пише | преброено днес |
| :---- | ---: |
| „правило 17 → 40 към 07.09" | **200** |
| „правило 29 → 14" | **141** |
| „правило 21 → 18" | **260** |

Правило 14 казва: „числата се БРОЯТ, не се преписват." Числата стоят там точно за да се
преброят — и не са преброявани от 07.09. **Няма машина, която да ги пази.** Това е ред за
ход 9.

---

## 5 · ЦЕЛИТЕ ЛИПСВАЩИ ПАРЧЕТА · потвърдени с брояч, не с довод

Вчерашният план твърдеше тези неща по извадка. Днес са проверени докрай:

| какво | как е броено | резултат |
| :---- | :---- | ---: |
| банково извлечение, четено от файл | всичките попадения на `izvlechenie` в `src/`+`app/` | **8, всичките декларации** |
| фирма · ЕИК · булстат | `grep -rniE` за трите думи в `src/ app/` | **0** |
| покана | `grep -rnio "pokana" src app` | **0** |
| коефициенти · диаграми | `find src app -iname "*koefitsient*" -o -iname "*diagram*"` | **0 файла** |
| папки и досиета | `grep -rn "papka" src app --include=*.ts` | **1** · декларацията |
| степен „Начало" на хелпа | `grep -rniE` за `ekspert` и `stepenNaHelpa` | **0** |
| изгледът на Журнала | `grep -rli "zhurnal" app/` | **0** |

**И едно, което е ПО-ЛОШО от липсващо:** колоната „Извлечение" в Кеша е обявена
`zatvorena: false` (`src/model/osnova.ts:625`) — тоест **пише се на ръка**. А това е точно
вторият независим път на правило 3 („парите минават по банка и по въведено"). Ръчно
въведеното извлечение не е втори път; то е същият път, написан два пъти.

---

## 6 · ДОКУМЕНТИТЕ ИЗОСТАВАТ ОТ КОДА НА 131 МЕСТА

Сто трийсет и един записа твърдят нещо, което кодът вече е опровергал. **Голямата им част са
в наша полза** — построено е повече, отколкото документът признава:

- **А4** · „тест няма за подписа върху `actor`" → `tests/vrata.test.ts:327` подменя САМО
  `actor` и очаква скъсана верига.
- **А14** · „Т5: при самия запис ✘" → `#akoEDrapnatKranat()` се вика на `vrata.ts:516`,
  последният ред преди носителя, вътре в цикъла на повторенията.
- **А18** · „`grep -cE 'NFC|normalize' tests/vrata.test.ts` → 0" → днес дава **7**.
- **В27** · „няма пазач" → петте команди на десетия тип живеят в
  `src/komandi/prozortsi/struktura.ts`.
- **Б37** · „сверката НАП я няма" → `src/smetach/nahodki-nap.ts:181-193` я прави и е жива.

**Двайсет и шест от 76-те адреса в регистъра** вече не сочат вярно. Портата `npm run registar`
пази точно тях, и е зелена — значи пази ФОРМАТА на адреса, не съдържанието му.

---

## 7 · КАКВО МЕНИ ТОВА В ПЛАНА

**Нищо в реда.** Ход 4 (правото) остава първи — днешното мерене го подсили: жив пазач имат
две от четирите оси.

**Ход 9 расте.** Към осемте му реда влизат:

1. обход 6 на `chistota` да лови невиканото през барела;
2. праг на двата брояча без праг (32 · 47);
3. правилото `agentat-ne-pishe` да сочи съществуващ път или да падне;
4. `tests/osemte.test.ts` да обхожда и `app/`;
5. машина за трите числа в `CLAUDE.md`;
6. `npm run registar` да проверява и СЪДЪРЖАНИЕТО на адреса, не само формата му.

**Един дълг пада** — Т35, измерен: строежът дава един изход.

**И една машина се разшири, докато този документ се пишеше.** Портата `npm run registar`
обяви пет от цитираните тук инварианта за „непознати въпроси". Причината е стара и записана в
самия ѝ код: ид-то на инвариант и белегът на въпрос живеят в ЕДНО пространство от букви и
числа, и досега машината ги различаваше по МЯСТО — ид-то на инвариант стои само в клетка на
таблица. Документ, който **обсъжда** инварианти, ги цитира в изречение, и мястото вече не
върши работа.

Разширено с ДУМАТА пред ид-то (`stroezh/registar.mjs`): „инвариант А24" не е въпрос, защото
белег на въпрос никога не се пише така. Стеснението беше по-тясно от езика — записано тук,
защото тиха промяна на машина е дефект (правило 32).

---

## 8 · КАКВО И ДНЕС НЕ Е ИЗМЕРЕНО

- **Верността на сметачите е започната, не завършена.** Един агент отвори 18 неща от
  17 файла · 3521 реда. Разминаването в „Резултат" е потвърдено; останалите сметачи чакат.
- **131-те остарели записа са НАМЕРЕНИ, не ПОПРАВЕНИ.** Поправката им е работа за ход 9;
  днешният документ е списъкът, не поправката.
- **Осемдесет и петте обратни дефекта не са класирани по тежест докрай** — 86 находки са
  обявени за носещи, от тях проверих лично пет.
